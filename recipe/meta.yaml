{% set version = "0.3.7" %}

package:
  name: usgs
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/u/usgs/usgs-{{ version }}.tar.gz
  sha256: 2592d1780089c1e52e79fded1cf73843310486eab46c5986248cc5aa96d3424d

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  entry_points:
    - usgs = usgs.scripts.cli:usgs

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - pip
  run:
    - python >={{ python_min }}
    - setuptools
    - click >=4.0
    - requests >=2.7.0
    - requests-futures >=0.9.5

test:
  imports:
    - usgs
    - usgs.scripts
  commands:
    - pip check
    - usgs --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/kapadia/usgs
  license: ISC
  license_file: LICENSE
  summary: Access the USGS inventory service

extra:
  recipe-maintainers:
    - ocefpaf
